{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "build:ui": "rm -f build && cd /Users/marcopdonoso/personalProjects/FullStackOpenParts/part2 && npm run build --prod && cp -r build /Users/marcopdonoso/personalProjects/FullStackOpenParts/part3",
    "deploy": "git push origin main",
    "deploy:full": "npm run build:ui && git add . & git commit -m uibuild && npm run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
